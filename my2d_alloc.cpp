#include <iostream>
#include <assert.h>

char** my2d_alloc(int rows, int cols)
{
  assert(rows > 0);
  assert(cols > 0);
}

int main()
{
  std::cout<<"Hello World!"<<std::endl;
  return 1;
}

